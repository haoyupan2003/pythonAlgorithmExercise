# Read inputs
d = int(input())   # distance to the capitol
n = int(input())   # number of students (not used)
t = int(input())   # temperature (not used)

# Calculate total miles (to the capitol and back)
total_miles = 2 * d

# Print result
print(total_miles)
